% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{per_capita}
\alias{per_capita}
\title{Normalise an indicator by population}
\usage{
per_capita(data, value, pop = NULL, suffix = "_per_capita", cache = TRUE)
}
\arguments{
\item{data}{A country-level (or panel) data frame with \code{iso3c}.}

\item{value}{The value column to normalise (unquoted).}

\item{pop}{Optional population column (unquoted). If absent, population is
fetched from WDI.}

\item{suffix}{Suffix for the new column (default \code{"_per_capita"}).}

\item{cache}{Whether to use the WDI cache when fetching population.}
}
\value{
\code{data} with a new per-capita column.
}
\description{
Removes the "is this map just a population map?" footgun by dividing a value
column by population. If no population column is supplied, \code{SP.POP.TOTL} is
pulled automatically for the relevant countries and years.
}
\examples{
df <- data.frame(iso3c = c("USA", "CHN"), year = 2020L,
                 co2 = c(5e6, 1e7), pop = c(331e6, 1402e6))
per_capita(df, co2, pop)
}
