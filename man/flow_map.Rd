% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{flow_map}
\alias{flow_map}
\title{Great-circle origin-destination flow map}
\usage{
flow_map(data, from, to, weight = NULL, origin = "country.name", n = 50)
}
\arguments{
\item{data}{An OD table.}

\item{from, to}{The origin and destination country columns (unquoted; names
or \code{iso3c}).}

\item{weight}{Optional column controlling arc width/alpha (unquoted).}

\item{origin}{How to read \code{from}/\code{to} (countrycode origin scheme).}

\item{n}{Points per arc (smoothness).}
}
\value{
A \code{ggplot} object.
}
\description{
Draws great-circle arcs between country pairs from an origin-destination
table (trade, migration, flights, remittances), resolving both endpoints to
centroids automatically.
}
\examples{
\donttest{
od <- data.frame(from = c("China", "Germany"),
                 to = c("United States", "France"),
                 value = c(500, 200))
if (requireNamespace("maps", quietly = TRUE)) {
  flow_map(od, from, to, value)
}
}
}
