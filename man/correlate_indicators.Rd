% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{correlate_indicators}
\alias{correlate_indicators}
\title{Pairwise correlation of indicators on the spine}
\usage{
correlate_indicators(data, ..., method = c("pearson", "spearman"), min_n = 3)
}
\arguments{
\item{data}{A country-level (or map-ready) data frame; polygon frames are
reduced to one row per country first.}

\item{...}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Indicator columns to
correlate. If empty, all numeric columns except coordinates, \code{year} and
other structural columns are used.}

\item{method}{\code{"pearson"} (default) or \code{"spearman"}.}

\item{min_n}{Minimum number of complete pairs for a correlation to be
reported (default \code{3}).}
}
\value{
A tibble with one row per indicator pair: \code{var_x}, \code{var_y}, \code{r},
\code{n} (complete pairs), sorted by \verb{|r|} descending.
}
\description{
Which indicators move together across countries? Computes pairwise
correlations between indicator columns (pairwise-complete, so patchy
coverage doesn't shrink every pair to the common subset), with the per-pair
\code{n} reported so a headline \code{r} computed on 12 countries can't masquerade as
a world fact.
}
\examples{
correlate_indicators(countryatlas::world_snapshot$countries)
}
