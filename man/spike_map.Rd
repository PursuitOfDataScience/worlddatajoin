% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{spike_map}
\alias{spike_map}
\title{Spike map (heights at country centroids)}
\usage{
spike_map(
  data,
  height,
  max_height = 20,
  width = 1.6,
  color = "#B2182B",
  alpha = 0.65
)
}
\arguments{
\item{data}{A country-level frame with \code{iso3c} and the \code{height} column.}

\item{height}{The column controlling spike height (unquoted).}

\item{max_height}{Height of the tallest spike, in degrees of latitude
(default \code{20}).}

\item{width}{Base width of each spike, in degrees of longitude (default
\code{1.6}).}

\item{color}{Spike colour (default a warm red).}

\item{alpha}{Spike fill transparency.}
}
\value{
A \code{ggplot} object.
}
\description{
The classic "population spikes" display: a triangular spike at each country
centroid whose height encodes the value. Like \code{\link[=bubble_map]{bubble_map()}} it is the
honest idiom for \emph{totals}, with a different visual trade-off: spikes
overplot less in dense regions (Europe, the Caribbean) because they only
grow upward. Uses the polygon backend, so it needs only \code{maps}.
}
\examples{
\donttest{
if (requireNamespace("maps", quietly = TRUE)) {
  spike_map(countryatlas::world_snapshot$countries, population)
}
}
}
