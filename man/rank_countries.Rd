% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{rank_countries}
\alias{rank_countries}
\title{Add rank, percentile and z-score}
\usage{
rank_countries(data, value, within = NULL, desc = TRUE)
}
\arguments{
\item{data}{A data frame.}

\item{value}{The value column to rank (unquoted).}

\item{within}{Optional grouping column(s) (unquoted or character) to rank
within.}

\item{desc}{Rank descending (largest = rank 1); default \code{TRUE}.}
}
\value{
\code{data} with \code{rank}, \code{percentile} and \code{z_score} columns added.
}
\description{
Adds \code{rank}, \code{percentile} and \code{z_score} for a value column, optionally within
a group (region, year, ...), for "top 10" tables and labelling.
}
\examples{
df <- data.frame(iso3c = c("USA", "CHN", "IND"), gdp = c(21, 17, 3))
rank_countries(df, gdp)
}
