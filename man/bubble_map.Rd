% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{bubble_map}
\alias{bubble_map}
\title{Proportional-symbol (bubble) map}
\usage{
bubble_map(
  data,
  size,
  color = NULL,
  projection = "equal_earth",
  backend = c("polygon", "sf"),
  max_size = 18,
  alpha = 0.7
)
}
\arguments{
\item{data}{A country-level frame with \code{iso3c} and the \code{size} column.}

\item{size}{The column controlling bubble size (unquoted).}

\item{color}{Optional column controlling bubble colour (unquoted).}

\item{projection}{Projection for the base map (sf path).}

\item{backend}{\code{"polygon"} (default) or \code{"sf"} for the base map.}

\item{max_size}{Largest bubble size.}

\item{alpha}{Bubble transparency.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots sized circles at country centroids -- the right idiom for \emph{totals}
(population, total emissions, total GDP), which a choropleth misrepresents
because big values hide in small countries and vice versa.
}
\examples{
\donttest{
snap <- worlddatajoin::world_snapshot$countries
if (requireNamespace("maps", quietly = TRUE)) {
  bubble_map(snap, population)
}
}
}
