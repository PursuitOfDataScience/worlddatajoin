% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{beta_convergence}
\alias{beta_convergence}
\title{Beta convergence (growth regression)}
\usage{
beta_convergence(data, value)
}
\arguments{
\item{data}{A panel with \code{iso3c} and \code{year}.}

\item{value}{The value column (unquoted); must be positive (log scale).}
}
\value{
A one-row tibble: \code{beta}, \code{se}, \code{t_value}, \code{p_value}, \code{r_squared},
\code{n} (countries), \code{speed} (annual convergence rate, \code{NA} when \code{beta >= 0})
and \code{half_life} (years). The fitted \link{lm} object is attached as the
\code{"model"} attribute.
}
\description{
Do poor countries grow faster than rich ones? The classic unconditional
beta-convergence test: each country's average log growth rate is regressed
on its log \emph{initial} level. A significantly negative \code{beta} is convergence;
the implied convergence \code{speed} and \code{half_life} (years to close half the
gap) are derived from it.
}
\examples{
set.seed(1)
start <- runif(20, 6, 11)                              # log initial level
growth <- 0.05 - 0.004 * start + rnorm(20, 0, 0.002)   # poorer grow faster
panel <- data.frame(
  iso3c = rep(sprintf("C\%02d", 1:20), each = 2),
  year  = rep(c(2000L, 2020L), 20),
  gdp   = as.vector(rbind(exp(start), exp(start + growth * 20)))
)
beta_convergence(panel, gdp)
}
\seealso{
\code{\link[=sigma_convergence]{sigma_convergence()}} for the dispersion-over-time counterpart.
}
