% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{bivariate_map}
\alias{bivariate_map}
\title{Two-variable bivariate choropleth}
\usage{
bivariate_map(
  data,
  fill_x,
  fill_y,
  palette = "GrPink",
  dim = 3,
  projection = "equal_earth"
)
}
\arguments{
\item{data}{An \code{sf} map-ready frame (use \code{geometry = "sf"}).}

\item{fill_x, fill_y}{The two value columns (unquoted).}

\item{palette}{A \code{biscale} palette name (default \code{"GrPink"}).}

\item{dim}{Bivariate dimension (2 or 3, default 3).}

\item{projection}{Projection.}
}
\value{
A \code{ggplot} object (the map; combine with \code{biscale::bi_legend()} for a
standalone legend).
}
\description{
A 2-D bivariate choropleth with a built-in 2-D legend (via the optional
\code{biscale} package), e.g. GDP per capita x life expectancy in one map.
}
\examples{
\dontrun{
world_data(2020, c(gdp = "NY.GDP.PCAP.KD", life = "SP.DYN.LE00.IN"),
           geometry = "sf") |>
  bivariate_map(gdp, life)
}
}
