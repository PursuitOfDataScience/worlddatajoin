% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{gini}
\alias{gini}
\title{Gini coefficient (population-weightable)}
\usage{
gini(x, weights = NULL, na.rm = TRUE)
}
\arguments{
\item{x}{A numeric vector (e.g. GDP per capita by country).}

\item{weights}{Optional non-negative weights (e.g. population), recycled
against \code{x} the usual R way. \code{NULL} (default) weights all values equally.}

\item{na.rm}{Whether to drop \code{NA} values (pairwise with their weight;
default \code{TRUE}).}
}
\value{
A single number in \verb{[0, 1]}: \code{0} is perfect equality.
}
\description{
The Gini index of inequality across countries, optionally weighted (weight
by population and the statistic describes inequality between \emph{people}
assigned their country's mean, not between country units).
}
\examples{
snap <- countryatlas::world_snapshot$countries
gini(snap$gdp_per_capita)                          # between countries
gini(snap$gdp_per_capita, weights = snap$population)  # between people
}
\seealso{
\code{\link[=theil]{theil()}}, which adds a between/within-group decomposition.
}
