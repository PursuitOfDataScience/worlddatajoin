# Curated indicator catalogue

A friendly-name to WDI-code lookup so
`indicator = common_indicators$population` beats memorising
`"SP.POP.TOTL"`.

## Usage

``` r
common_indicators
```

## Format

A tibble with columns `name` (friendly name), `code` (WDI indicator
code) and `description`.

## Source

World Bank indicator catalogue.
