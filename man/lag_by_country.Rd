% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{lag_by_country}
\alias{lag_by_country}
\alias{diff_by_country}
\title{Panel lag / difference by country}
\usage{
lag_by_country(data, value, n = 1, suffix = NULL)

diff_by_country(data, value, n = 1, suffix = NULL)
}
\arguments{
\item{data}{A panel with \code{iso3c} and \code{year}.}

\item{value}{The value column (unquoted).}

\item{n}{Number of periods to lag / difference over (default \code{1}).}

\item{suffix}{Suffix for the new column. Defaults to \code{"_lag"} / \code{"_diff"}
(with \code{n} appended when \code{n > 1}, e.g. \code{"_lag5"}).}
}
\value{
\code{data} with the lagged / differenced column added.
}
\description{
The two panel primitives everyone hand-rolls (and gets subtly wrong when the
frame isn't sorted): the value \code{n} years back, and the change since then --
grouped by \code{iso3c}, ordered by \code{year}, so country A's 1960 never leaks into
country B's first row.
}
\examples{
df <- data.frame(iso3c = "USA", year = 2000:2003, gdp = c(100, 110, 121, 133))
lag_by_country(df, gdp)
diff_by_country(df, gdp)
}
