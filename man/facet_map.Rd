% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{facet_map}
\alias{facet_map}
\title{Small-multiple choropleths}
\usage{
facet_map(data, fill, facet, ncol = NULL, ...)
}
\arguments{
\item{data}{A map-ready frame (polygon or sf) containing the \code{facet} column.}

\item{fill}{The fill column (unquoted).}

\item{facet}{The faceting column (unquoted; e.g. \code{year} or \code{continent}).}

\item{ncol}{Number of facet columns (passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}).}

\item{...}{Passed to \code{\link[=world_map]{world_map()}} (e.g. \code{style}, \code{projection}).}
}
\value{
A faceted \code{ggplot} object.
}
\description{
Facet a choropleth into small multiples (one panel per group or per year) --
the static counterpart to \code{\link[=animate_world]{animate_world()}}, for print and side-by-side
comparison. Builds a \code{\link[=world_map]{world_map()}} and facets it on \code{facet}.
}
\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
if (requireNamespace("maps", quietly = TRUE)) {
  mapdf <- attach_geometry(snap, geometry = "polygon")
  facet_map(mapdf, gdp_per_capita, continent, style = "quantile")
}
}
}
