% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize.R
\name{standardize_country}
\alias{standardize_country}
\title{Add ISO codes and classifications to any data frame}
\usage{
standardize_country(
  data,
  country_col,
  origin = "country.name",
  add = c("iso3c", "iso2c", "continent", "region"),
  custom_match = wdj_overrides(),
  warn = TRUE
)
}
\arguments{
\item{data}{A data frame / tibble.}

\item{country_col}{The column holding country names or codes
(unquoted, tidy-eval).}

\item{origin}{How to read \code{country_col}; any \code{\link[countrycode:countrycode]{countrycode::countrycode()}}
origin scheme such as \code{"country.name"} (default), \code{"iso2c"}, \code{"iso3c"},
\code{"wb"}, \code{"un"}.}

\item{add}{Character vector of attributes to add. Defaults to
\code{c("iso3c", "iso2c", "continent", "region")}. Any countrycode destination
is accepted, plus the shortcuts \code{"flag"}, \code{"currency"}, \code{"tld"}.}

\item{custom_match}{A named character vector of name -> iso3c overrides;
defaults to \code{\link[=wdj_overrides]{wdj_overrides()}}. Merged on top of the built-in matching.}

\item{warn}{Whether to warn about unmatched countries (default \code{TRUE}).}
}
\value{
\code{data} with the requested columns added (and existing same-named
columns overwritten).
}
\description{
The package's mission, exposed for \emph{your} data: take a data frame keyed on
messy country names (or codes) and attach standardised ISO codes plus useful
classifications, reconciling spellings via \code{\link[countrycode:countrycode]{countrycode::countrycode()}} and
the curated \code{\link[=wdj_overrides]{wdj_overrides()}} table. The result joins cleanly to anything
else keyed on \code{iso3c}.
}
\examples{
df <- data.frame(nation = c("U.S.", "S. Korea", "Czechia"), value = 1:3)
standardize_country(df, nation)
}
