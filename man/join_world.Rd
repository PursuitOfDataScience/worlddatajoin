% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{join_world}
\alias{join_world}
\title{One call: your data, on a map}
\usage{
join_world(
  data,
  country_col = NULL,
  origin = "country.name",
  geometry = c("polygon", "sf", "none"),
  scale = "small",
  region = NULL,
  projection = "equal_earth",
  recenter = NULL,
  warn = TRUE
)
}
\arguments{
\item{data}{A data frame keyed on country names or codes.}

\item{country_col}{The country column (unquoted). If omitted, it is
auto-detected.}

\item{origin}{How to read \code{country_col} (any countrycode origin scheme).}

\item{geometry}{\code{"polygon"} (default), \code{"sf"} or \code{"none"}.}

\item{scale}{Natural Earth resolution for the \code{sf} backend.}

\item{region}{Optional region subset (see \code{\link[=world_geometry]{world_geometry()}}).}

\item{projection, recenter}{Projection options for the \code{sf} backend.}

\item{warn}{Whether to report unmatched countries (default \code{TRUE}); also
surfaces a \code{\link[=check_country_match]{check_country_match()}} summary.}
}
\value{
A plot-ready frame: polygon tibble, \code{sf} object, or (for
\code{geometry = "none"}) the standardised table.
}
\description{
Auto-detects the country column, standardises it to ISO codes (via
\code{\link[=standardize_country]{standardize_country()}}), attaches geometry and returns a plot-ready frame --
the function that fulfils the package's promise for \emph{your} own data. Pipe the
result straight into \code{\link[=world_map]{world_map()}}.
}
\examples{
rates <- data.frame(country = c("United States", "Brazil", "Kenya"),
                    vaccination_pct = c(0.7, 0.8, 0.6))
\donttest{
if (requireNamespace("maps", quietly = TRUE)) {
  joined <- join_world(rates, country)
}
}
}
