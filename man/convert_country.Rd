% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{convert_country}
\alias{convert_country}
\title{Friendly country-code conversion}
\usage{
convert_country(
  x,
  to = "iso3c",
  from = "country.name",
  custom_match = wdj_overrides(),
  warn = TRUE
)
}
\arguments{
\item{x}{A vector of country names or codes.}

\item{to}{Destination scheme. A shortcut (\code{"iso3c"}, \code{"flag"}, \code{"currency"},
\code{"tld"}, \code{"continent"}, \code{"region"}, \code{"cown"}, ...), a localized name
\code{"name_<lang>"} (\code{"name_fr"}, \code{"name_es"}, \code{"name_zh"}, ... -- any
language in countrycode's CLDR tables), or any raw countrycode
destination.}

\item{from}{Origin scheme (default \code{"country.name"}).}

\item{custom_match}{Optional overrides (default \code{\link[=wdj_overrides]{wdj_overrides()}}).}

\item{warn}{Whether to warn about unmatched inputs.}
}
\value{
A vector of converted codes.
}
\description{
A discoverable wrapper around \code{\link[countrycode:countrycode]{countrycode::countrycode()}} exposing the full
set of schemes with first-class shortcuts for the high-value ones: flag
emoji, currency, top-level domain, continent/region and research codes
(Correlates of War, Polity, Gleditsch-Ward, V-Dem, IMF, FAO, FIPS, GAUL).
}
\examples{
convert_country(c("Japan", "Brazil"), to = "flag")
convert_country("Germany", to = "currency")
convert_country(c("USA", "France"), to = "continent")
convert_country(c("Germany", "United States"), to = "name_fr")
}
