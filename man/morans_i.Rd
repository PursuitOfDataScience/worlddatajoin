% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{morans_i}
\alias{morans_i}
\title{Global Moran's I (spatial autocorrelation)}
\usage{
morans_i(data, value, scale = "small", n_perm = 999)
}
\arguments{
\item{data}{A country-level data frame with \code{iso3c} (map-ready frames are
reduced to one row per country).}

\item{value}{The value column (unquoted).}

\item{scale}{Natural Earth resolution for the adjacency (see
\code{\link[=country_borders]{country_borders()}}).}

\item{n_perm}{Number of permutations for the pseudo-p-value (default \code{999};
use \code{0} to skip the test).}
}
\value{
A one-row tibble: \code{i} (observed Moran's I), \code{expected}
(\eqn{-1/(n-1)} under no autocorrelation), \code{n} (countries used),
\code{n_links} (border pairs among them) and \code{p_value} (one-sided,
\eqn{P(I_{perm} \ge I_{obs})}; positive autocorrelation is the standard
alternative). Set a seed beforehand for a reproducible \code{p_value}.
}
\description{
Do neighbouring countries have similar values? Global Moran's I on the
country spine, using the \code{\link[=country_borders]{country_borders()}} land-border adjacency as the
spatial weights (row-standardised), with a permutation pseudo-p-value. No
\code{spdep} required: at ~200 countries the dense arithmetic is trivial, and
reusing the package's own adjacency keeps the weights consistent with the
maps. Countries with no land border in the data (islands) carry no weight
and are excluded.
}
\examples{
\dontrun{
snap <- countryatlas::world_snapshot$countries
set.seed(42)
morans_i(snap, gdp_per_capita)   # GDP clusters strongly in space
}
}
