% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{growth_rate}
\alias{growth_rate}
\title{Year-on-year (or compound) growth rate}
\usage{
growth_rate(data, value, type = c("yoy", "cagr"), suffix = "_growth")
}
\arguments{
\item{data}{A panel with \code{iso3c} and \code{year}.}

\item{value}{The value column (unquoted).}

\item{type}{\code{"yoy"} (default, period-over-period) or \code{"cagr"} (compound
annual growth rate vs. the first non-\code{NA} year).}

\item{suffix}{Suffix for the new column (default \code{"_growth"}).}
}
\value{
\code{data} with a growth-rate column added (a proportion, so 0.03 = 3\%).
}
\description{
Adds a growth-rate column to a panel: either the period-over-period change
(\code{"yoy"}) or the compound annual growth rate from the first observed year
(\code{"cagr"}), computed per country.
}
\examples{
df <- data.frame(iso3c = "USA", year = 2000:2002, gdp = c(100, 110, 121))
growth_rate(df, gdp)
}
