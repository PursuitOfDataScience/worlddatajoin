% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{locate_country}
\alias{locate_country}
\title{Tag coordinates with the country that contains them}
\usage{
locate_country(
  lon = NULL,
  lat = NULL,
  points = NULL,
  scale = "small",
  add = "country",
  tolerance_km = 25
)
}
\arguments{
\item{lon, lat}{Numeric vectors of longitude / latitude (recycled together;
ignored if \code{points} is supplied).}

\item{points}{Optional \code{sf} POINT object to use instead of \code{lon}/\code{lat}.}

\item{scale}{Natural Earth resolution for the lookup geometry.}

\item{add}{Extra attributes to return alongside \code{iso3c} (any
\code{\link[=convert_country]{convert_country()}} destination, e.g. \code{"country"}, \code{"continent"}).}

\item{tolerance_km}{Snap an unmatched point to the nearest country when it
lies within this many kilometres of one (default \code{25}). Coarse (110m)
coastlines place some genuinely-onshore coastal points just outside their
country (New York sits ~0.5 km beyond the simplified US coast); this
rescues them while leaving open-ocean points \code{NA} (the nearest land is
hundreds of km away). Set \code{0} for a strict point-in-polygon lookup.}
}
\value{
A tibble with one row per point: \code{iso3c} plus any \code{add} columns
(\code{NA} for points that fall in no country, e.g. open ocean).
}
\description{
Point-in-polygon lookup: given longitude / latitude vectors (or an \code{sf} POINT
object), return the \code{iso3c} of the country each point falls in -- the bridge
for getting point data (events, stations, observations) onto the country
spine so it can be joined, aggregated and mapped like everything else.
}
\examples{
\dontrun{
locate_country(lon = c(2.35, -74.0), lat = c(48.85, 40.7))  # Paris, NYC
}
}
