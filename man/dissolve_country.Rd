% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical.R
\name{dissolve_country}
\alias{dissolve_country}
\title{Resolve dissolved entities to their successor states}
\usage{
dissolve_country(x, warn = TRUE)
}
\arguments{
\item{x}{A character vector of country names (historical and/or modern).}

\item{warn}{Whether to warn about names that match neither a historical
entity nor a modern country (default \code{TRUE}).}
}
\value{
A tibble with one row per (input, successor) pair: \code{input} (as
given), \code{historical} (canonical dissolved-entity name, \code{NA} for modern
countries), \code{dissolved} (year the entity ceased to exist, \code{NA} for
modern), \code{iso3c} and \code{country} (the successor state). Unmatched inputs
yield one row with \code{iso3c = NA}.
}
\description{
Historical data is full of countries that no longer exist -- the USSR,
Yugoslavia, Czechoslovakia -- and they poison naive joins twice over: most
are silently unmatched, and some are silently \emph{mis}matched
(\code{countrycode} resolves \code{"USSR"} to Russia alone, so Soviet-era totals
quietly become Russian totals). \code{dissolve_country()} resolves a mixed
vector of historical and modern names against the curated
\link{historical_codes} crosswalk: dissolved entities expand to one row per
successor state (one-to-many, dated), while modern names pass through as
single rows -- so a whole messy column can be piped in unchanged.
}
\examples{
dissolve_country(c("USSR", "Czechoslovakia", "France"))
# One-to-many: Yugoslavia expands to its successor territories
dissolve_country("Yugoslavia")
}
\seealso{
\link{historical_codes} for the crosswalk itself and the successor
policy (e.g. Kosovo's inclusion in the Yugoslavia list);
\code{\link[=check_country_match]{check_country_match()}}, whose \code{historical} column flags these entities.
}
