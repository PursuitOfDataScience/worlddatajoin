% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{country_codes}
\alias{country_codes}
\title{The countrycode codelist as a tidy tibble}
\usage{
country_codes(codes = NULL)
}
\arguments{
\item{codes}{Optional character vector of column names to keep (in addition
to \code{iso3c}). If \code{NULL}, a useful default subset is returned.}
}
\value{
A tibble, one row per country.
}
\description{
The whole \link[countrycode:codelist]{countrycode::codelist} reshaped into a tidy, pipeable lookup you
can \code{filter()} / \code{join()} directly -- one row per country.
}
\examples{
country_codes()
country_codes(c("iso2c", "continent", "currency"))
}
