% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{complete_years}
\alias{complete_years}
\title{Fill or interpolate panel gaps}
\usage{
complete_years(
  data,
  years = NULL,
  value = NULL,
  method = c("none", "locf", "linear")
)
}
\arguments{
\item{data}{A panel with \code{iso3c} and \code{year}.}

\item{years}{The full set of years to complete to. Defaults to the observed
min:max.}

\item{value}{Optional value column(s) (character) to fill. If \code{NULL}, all
numeric columns except \code{year} are filled.}

\item{method}{\code{"none"} (default; just complete the grid), \code{"locf"} or
\code{"linear"}.}
}
\value{
A completed (and optionally filled) panel tibble.
}
\description{
Completes a panel so every country has every year, optionally filling missing
values by carry-forward (\code{"locf"}) or linear interpolation (\code{"linear"}) so
animations do not flicker on missing years.
}
\examples{
df <- data.frame(iso3c = "USA", year = c(2000L, 2002L), gdp = c(1, 3))
complete_years(df, 2000:2002, method = "linear")
}
