% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{dorling_map}
\alias{dorling_map}
\title{Dorling cartogram (first-class verb)}
\usage{
dorling_map(
  data,
  weight,
  fill = NULL,
  k = 5,
  itermax = 1000,
  projection = "equal_earth"
)
}
\arguments{
\item{data}{An \code{sf} map-ready frame.}

\item{weight}{The column controlling circle size (unquoted).}

\item{fill}{Optional fill column (unquoted); defaults to \code{weight}.}

\item{k}{Share of the bounding box filled by the largest circle (default
\code{5}; passed to \code{cartogram::cartogram_dorling()}).}

\item{itermax}{Maximum iterations of the circle-repulsion algorithm
(default \code{1000}; raise it if circles still overlap in the result).}

\item{projection}{Projection (an equal-area CRS is recommended).}
}
\value{
A \code{ggplot} object.
}
\description{
Non-overlapping proportional circles sized by \code{weight}, positioned to stay
as close as possible to each country's true location -- arguably the most
legible cartogram variant, since a microstate's circle is exactly as
visible as a giant country's. A first-class verb for
\code{\link[=cartogram_map]{cartogram_map()}}\code{(type = "dorling")} that surfaces the Dorling-specific
tuning knobs.
}
\examples{
\dontrun{
world_data(2020, c(pop = "SP.POP.TOTL"), geometry = "sf") |>
  dorling_map(pop)
}
}
